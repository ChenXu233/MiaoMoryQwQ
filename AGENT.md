# AGENT.md — MiaoMory 项目规范（最高事实源）

- 状态：现行
- 维护者：项目所有者
- 最后更新：2026-09-04
- 适用范围：本仓库的所有人类协作者与 AI 编码代理

本文件是项目规范的最高事实源。任何文档与本文件冲突时，以本文件为准，并同步修正冲突文档。

## 1. 项目是什么

**MiaoMory**：个人媒体语义索引引擎。定位为"胶水/感知引擎"——摄取、分析、索引与检索个人照片/视频/音频，不存储原始媒体文件。

- 技术栈：Tauri v2（桌面优先）+ Rust + Vue 3/TypeScript + SQLite/sqlite-vec + ONNX Runtime（ort）（ADR-0009）
- 当前阶段：P0 初始化中（代码骨架搭建）
- 产品与架构基线：`docs/whitepaper/whitepaperv1.md`（内容版本 v0.2）

## 2. 铁律（不可协商）

1. **规范先行**：任何设计、范围或依赖变更，先更新对应文档，再动代码；PR 必须包含相应文档更新。
2. **决策走 ADR**：影响架构/依赖/范围的决策必须先落 `docs/decisions/`；已接受的 ADR 不可改写，只能以新 ADR supersede。
3. **保留 sqlite-vec**（ADR-0002）：不引入"BLOB + 内存暴力扫描"等自研检索算法方案；索引与检索算法由数据库扩展承担。
4. **MVP 边界**（ADR-0003）：桌面优先；移动端 / S3 / PG / 人脸 / 插件 / MCP / 音视频语义为 LATER；越界需新 ADR。
5. **性能验证策略**（ADR-0004）：CI 不跑常驻语义检索/万级 bench；性能验收为一次性人工基准；种子数据生成器延后，测试数据由项目所有者人工提供。
6. **类型契约**：Rust 命令为唯一事实源，specta 生成 TS 类型；禁止手写 IPC 类型。
7. **依赖克制**：新依赖须在 PR 描述中说明服务哪条体验主线；框架级依赖需 ADR。

## 3. 文档地图

| 目录 | 类型 | 读者 | 规范出处 |
| :--- | :--- | :--- | :--- |
| `AGENT.md` | 项目规范 | 全体 | 本文件 |
| `docs/whitepaper/` | 白皮书（版本化 vX.Y） | 全体 | 白皮书附录 B |
| `docs/decisions/` | 架构决策记录（ADR） | 全体 | 本文件 §5 |
| `docs/spec/` | 功能规格（行为） | 开发者/测试 | `docs/spec/README.md` |
| `docs/ux/` | 体验规格（质量） | 全体 | `docs/ux/README.md` |
| `docs/developers/` | 开发者文档 | 开发者 | `docs/developers/README.md` |
| `docs/users/` | 用户文档 | 使用者 | `docs/users/README.md` |
| `README.md` / `CONTRIBUTING.md` | 入口 | 全体 | 本文件 §4.1 |

## 4. 文档编写规范

### 4.1 通用格式

- **语言**：文档一律中文；代码标识符与命令英文；代码注释中英均可，但同一文件保持一致。
- **格式**：GFM；ATX 标题；表格必须对齐；路径一律用行内代码 `path`。
- **命名**：文件名 kebab-case；ADR 为 `NNNN-小写-slug.md`；图片放同目录 `assets/`。
- **头部元数据**（README 类入口文件除外，所有规范/规格/指南类文档必须包含）：

```markdown
状态：现行 | 起草 | 已废弃
维护者：
最后更新：YYYY-MM-DD
```

### 4.2 六类文档规范摘要

| 类型 | 要点 |
| :--- | :--- |
| 白皮书 | 版本化 vX.Y；每次变更写修订记录；只写产品/选型/架构/边界，不写实现细节 |
| ADR | 见 §5；历史不可改写 |
| 功能规格 | 一功能一份；必须有目标/非目标、流程、状态矩阵、验收标准（Given/When/Then）；模板见 `docs/spec/_template.md` |
| 体验规格 | 与功能规格同编号对应；延迟预算、状态矩阵（体验视角）、文案、键盘/无障碍；标准见 `docs/ux/README.md` |
| 开发者文档 | how-to 导向；每篇回答"什么/为什么/怎么做"；不贴大段代码代替说明 |
| 用户文档 | 面向非技术用户；每页一个任务；禁止裸技术词（Docker/SQLite/WAL 等） |

### 4.3 索引登记

新增/删除/废弃任何文档，必须同步更新 `docs/README.md` 的文档地图；目录结构变更需先讨论（必要时落 ADR）。

## 5. ADR 流程

- **何时写**：架构、依赖、范围、流程、性能策略的决策；个人风格类问题不必写。
- **状态机**：`Proposed` → `Accepted` / `Rejected`；Accepted 可被 `Superseded` / `Deprecated`（写新 ADR，不改旧文）。
- **编号**：`NNNN` 四位递增；文件名 `NNNN-小写-slug.md`；模板 `docs/decisions/_template.md`。
- **索引**：新 ADR 必须登记进 `docs/decisions/README.md` 状态表。
- **评审**：项目所有者批准后方可 Accepted；AI 代理可起草（Proposed），不得自行 Accepted。

## 6. 代码与提交规范（代码启动后立即生效）

- **提交**：Conventional Commits（`feat/fix/docs/refactor/chore/test`），scope 用 crate 名或 docs 分类；一个提交只做一件事。
- **目录与依赖规则**：以 ADR-0005 为准（core 零依赖、单向依赖、全项目 5~6 个端口 trait）。
- **PR**：一个 PR 一个垂直切片；DoD 见 §7。
- **分支**：`main` 保护；功能分支 `feature/<slice>`；文档小修可直接进 main。

## 7. 开发工作流（垂直切片，ADR-0007）

| 阶段 | 内容 | 体验验收 |
| :--- | :--- | :--- |
| P0 | workspace 骨架 + 契约生成（specta）+ 更新器 + CI | 类型安全 IPC 成为日常 |
| P1 | 导入 → 缩略图 → 时间轴浏览（无 AI） | 进度可暂停/恢复；坏文件隔离 |
| P2 | 模型下载 → 嵌入 → 中文语义搜索 | 状态矩阵齐全；延迟预算达标 |
| P3 | FTS/过滤/RRF 融合 + 键盘操作 + 打磨 | 发布首个 alpha |

**切片 DoD（Definition of Done）**：

1. 文档先行：涉及的功能规格/体验规格已写并登记索引
2. 实现：Rust 命令/查询 + pipeline 阶段 + UI + 单元测试
3. 验收：TDD 单元测试 + 项目所有者手工验证清单（e2e 自动化延后，ADR-0010）
4. 质量：lint/clippy/tsc 通过；契约生成无漂移
5. 性能：涉及模型/索引变更时，一次性人工基准已记录

**发布节奏**：alpha 起每周构建 + 自动更新器推送；tracing 日志落盘 + 开发版性能浮层。

## 8. 质量与验证策略（ADR-0004）

- **CI 常驻**：格式 / lint / clippy / tsc / 单元测试 / 契约生成一致性 / 三平台构建矩阵（e2e 延后，ADR-0010）。
- **一次性人工基准**：模型推理速度、sqlite-vec 目标规模验收；结果记录 `docs/ux/latency-budget.md`；模型 / ORT / sqlite-vec / Tauri 大版本升级时触发重验。
- **禁止清单**：常驻 10k 合成照片 bench；合成种子数据生成器（延后）。
- **性能问题归因顺序**：先查我们的代码（契约开销、N+1、解码管线），再查模型，最后才怀疑数据库。

## 9. 给 AI 代理的特别约定

1. 动工前必读：本文件、`docs/README.md`、相关 ADR。
2. 涉及设计时先查白皮书 §2 MVP 边界；越界先用提问工具确认，不擅自扩大范围。
3. 修改文档必须同步索引（`docs/README.md`）；不改写 ADR 历史。
4. 不新增依赖、不新增顶层目录、不改动铁律，除非用户明确指示。
5. 任务完成后在回复中列出全部产出/变更文件路径。
6. 文档内容不得虚构测量数据：所有数字必须来自实测，或标注"待基准"。

## 10. 规范演进

本文件与 ADR 的变更本身需按规范走：小修（错别字/链接）直接提交；实质变更需新 ADR 或项目所有者明确指示。
