# 贡献指南

本项目**规范先行**：先写文档，再写代码。所有贡献从 `AGENT.md` 开始。

## 五步流程

1. **读规范**：`AGENT.md` 与 `docs/README.md`（铁律、文档地图、ADR 流程）。
2. **提变更**：功能/范围/依赖变更先落 ADR 与功能规格（`docs/spec/`），再动手。
3. **文档先行**：先写功能规格与体验规格，再写实现。
4. **垂直切片**：一个 PR 一个切片（ADR-0007 的 DoD）。
5. **提交**：Conventional Commits；文档更新随 PR 同步登记 `docs/README.md`。

## PR 清单

- [ ] 涉及文档已更新，并登记进 `docs/README.md`
- [ ] 无新依赖；若有：PR 描述说明服务哪条体验主线（框架级需 ADR）
- [ ] CI 绿：lint / clippy / tsc / 单元测试 / e2e
- [ ] 涉及模型或索引变更：一次性基准结果已记录（ADR-0004）

## 行为准则

- 尊重 MVP 边界（ADR-0003），越界先讨论
- 数字必须实测或标注"待基准"，不虚构
- 疑问先问，不擅自扩大范围
